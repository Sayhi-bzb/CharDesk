---
title: Dialog Surfaces
description: Contract for Dialog, AlertDialog, Sheet, and modal Host elevation.
---

- Use shared Dialog section components. Instances may set width but do not
  redefine shell, spacing, or close geometry.
- The shell owns spacing and padding. Header, body, and footer add no surface,
  divider, or elevation.
- Group content through spacing and typography, not accent panels or cards.
- AlertDialog shares the surface contract without close-control space.
  Destructive semantics belong to its confirmation action.
- Sheet and mobile Sidebar panels retain the modal surface and elevation defined
  by the [Visual system](/development/host-ui/visual-system).
- Embedded controls do not add nested shadows.
- Navigation settings use one Dialog with sidebar navigation from medium widths
  upward and Select navigation on phones.
  Navigation does not own a scroll area; long labels truncate within their item.
- Settings sections render content panels, not nested Dialog shells. Each panel
  owns its vertical scroll region while the navigation and close control remain
  visible. Settings data tables fit the panel width; cells truncate instead of
  creating panel-level horizontal scrolling.
  The Dialog title is visually hidden; the active section heading is the only
  visible heading. Settings rows stay single-line and use spacing and hover
  state instead of decorative separators.

Close-control geometry belongs to [Core Host icon controls](/development/host-ui/icon-controls).

## Authority and evidence

- Composition: [`recipes.ts`](https://github.com/Sayhi-bzb/CharDesk/blob/main/packages/ui/src/recipes.ts),
  [`dialog.tsx`](https://github.com/Sayhi-bzb/CharDesk/blob/main/packages/ui/src/dialog.tsx),
  [`alert-dialog.tsx`](https://github.com/Sayhi-bzb/CharDesk/blob/main/packages/ui/src/alert-dialog.tsx),
  and [`sheet.tsx`](https://github.com/Sayhi-bzb/CharDesk/blob/main/packages/ui/src/sheet.tsx)
- Shared contract: [`dialog.test.tsx`](https://github.com/Sayhi-bzb/CharDesk/blob/main/packages/ui/src/dialog.test.tsx)
- Mobile Sheet flow: [`apps/canvas/e2e/sidebar-collapse.spec.ts`](https://github.com/Sayhi-bzb/CharDesk/blob/main/apps/canvas/e2e/sidebar-collapse.spec.ts)
