# 上游事实卡片规则

[返回事实白板](../README.md)

每张卡片只登记四类当前事实：本项目关系、已消费能力、明确边界、权威来源。

- 依赖与版本由 `package.json` 和 lockfile 持有，卡片不复制版本号。
- 已实现行为链接源码、package README 或自动化测试，不重复类型和测试矩阵。
- 未进入依赖图的项目只记录本地边界，不跟踪外部路线图。
- 优势、风险比较、评分、调研轮次、采用门槛和被替代方案只保留在 Git 历史。
- 新事实若已有所有者，应更新所有者并从卡片链接，不建立第二份描述。
