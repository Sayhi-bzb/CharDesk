import { DEFAULT_CHARDESK_CELL_METRICS } from "@chardesk/rendering";

export const DEFAULT_GRID_GEOMETRY = Object.freeze({
  cellWidth: DEFAULT_CHARDESK_CELL_METRICS.cellWidth,
  cellHeight: DEFAULT_CHARDESK_CELL_METRICS.cellHeight,
  baseline: DEFAULT_CHARDESK_CELL_METRICS.baseline,
});
